async function request(method,args){const response=await fetch('/bridge',{method:'POST',headers:{'Content-Type':'application/json','X-Ongi-Request':'studio'},body:JSON.stringify({method,args})});const value=await response.json();if(!value.ok)throw new Error(value.error);return value.data;}
export const bridge=typeof window!=='undefined'&&window.ongi?window.ongi:{state:()=>request('state'),importPhotos:files=>request('importPhotos',files),importArtwork:f=>request('importArtwork',f),asset:id=>request('asset',id),start:r=>request('start',r),job:id=>request('job',id),cancel:id=>request('cancel',id),saveKey:r=>request('saveKey',r),pixelPreview:id=>request('pixelPreview',id),export:async r=>{const out=await request('export',r);const link=document.createElement('a');link.href=`data:${out.mime};base64,${out.base64}`;link.download=`ongi-${r.format}.${out.ext}`;link.click();return {saved:true};}};
